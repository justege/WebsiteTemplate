

export interface Window {
    APP_CONFIG: {
        API_BASE: string;
        API_HOST: string;
        DEPLOY_TARGET: string;
        FEATURE_FLAGS: string[];
        FILE_SIZE_LIMIT: number;
        };
    }
